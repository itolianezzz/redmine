<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ru.spb.itolia.redmine"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk android:minSdkVersion="8" />
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:icon="@drawable/redmine"
        android:label="@string/app_name" android:uiOptions="splitActionBarWhenNarrow" android:logo="@drawable/redmine">
        <activity android:name=".ui.LoginActivity" android:label="@string/app_name">
           <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity><activity android:name=".ui.ProjectsActivity" android:launchMode="standard">
 
        </activity><activity
            android:name=".ui.DashboardActivity"
            android:label="@string/app_name" >
        </activity>
        <activity android:name=".ui.IssuesActivity"></activity>
        <activity android:name=".ui.NewIssueActivity"></activity>
        <activity android:name=".ui.RedmineHostsActivity"></activity>
        
        
    </application>

</manifest>